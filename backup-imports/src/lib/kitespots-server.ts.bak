// This file re-exports functions from the api-service for backward compatibility
import { 
  getKiteSpotByName, 
  getAllKiteSpots, 
  getKiteSpotForecast,
  KiteSpotForecast
} from "@/services/api-service";
;

// You can add any server-specific logic here if needed


// Re-export for backward compatibility
export async function getKiteSpots(...args) {
  console.warn('getKiteSpots from kitespots-server.ts is deprecated. Use the client-side API service instead.');
  return getKiteSpots(...args);
}